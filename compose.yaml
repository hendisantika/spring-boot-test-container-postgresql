services:
  postgres:
    container_name: blog_postgres_db
    image: 'postgres:17-alpine3.20'
    environment:
      - 'POSTGRES_DB=blog'
      - 'POSTGRES_USER=yuji'
      - 'POSTGRES_PASSWORD=53cret'
    ports:
      - '5432'
